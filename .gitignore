logs/
__pycache__/
*.pyc
.pytest_cache/
data/
.venv/
.vscode/
.ignored/
# Worktrees live here; never part of the tree itself.
.claude/worktrees/
# Defense in depth: these must never land in a public repo even outside .ignored/.
*service-account*.json
client_secret_*.json
# The upload lock and its holder record (upload_lock.py).
.upload.lock
.upload.holder.json
