# Requires Python 3.10+ (google-auth, google-api-core). macOS system python3 is 3.9.6.
# Exact pin: retry handling relies on library internals. See docs/decisions/QUOTA-AND-RUNS.md.
internetarchive==5.11.1
urllib3>=2.0,<3
pytest>=8.0
ruff>=0.15
pyright>=1.1
google-api-python-client>=2.198
google-auth[requests]>=2.56
